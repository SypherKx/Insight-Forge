{
  "buildCommand": "cd frontend && npm install && npx vite build --config vite.config.vercel.ts",
  "outputDirectory": "frontend/dist",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
